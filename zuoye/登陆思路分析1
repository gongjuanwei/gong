功能：
		1、需要判断为空
		2、最多输入3次
		3、提示用户不存在要判断
		4、错误次数达到3要提示
		5、一个用list、一个用字典
usernames = ["admin","test","dev"]
password = ["111","222","333"]
	思路分析：
		1、循环3次
			for  i  in range(3)
		2、输入账号和密码
			username =input("username:")
			password =input("password:")
		3、判断输入是否为空
			strip:去空格
				if   len(username.strip()) ==0   or  len(password)==0:
					print("账号和密码不能为空")
			isspace:判断是不是空格，不能判断为空
			isspace and len(xx)==0：isspace和len组合判断

		4、判断一个账号是否存在
			用count和in都行
				elif  username not  in  usernames:
					print("用户名不存在")
		5、如果账号存在
			判断输入的密码和存的密码是否一致
			dict
				通过username取vlaue，判断密码
			list
				查找username的下标，然后拿下标去password ：list里面取到password
					else:
      username_index =usernames.index(username)
     db_password= passwords[username_index]
     if password ==db_password:
        print("欢迎登陆%s,今天日期是%S"%（username，datetime.datetime.today()）)
        break
   else:
     print("密码错误！")
		6、错误次数上限提升
			用循环对应else
				else：
       print（“错误次数达到上限！”）